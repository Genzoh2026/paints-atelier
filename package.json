{
  "name": "paints-atelier",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local stroke-based repainting with seven media, mixed-media layers and process recording.",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node scripts/check.mjs",
    "test:browser": "node tests/browser.mjs"
  },
  "engines": { "node": ">=20" }
}
